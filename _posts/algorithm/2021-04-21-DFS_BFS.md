---
title: 깊이우선 탐색 & 너비우선 탐색
categories: [algorithm]
comments: true
---

그래프를 탐색하는 방법으로 `깊이우선 탐색` 과 `너비우선 탐색` 이 있습니다. 알고리즘 문제를 풀면서 개념을 다시한번 정리하고자 작성했습니다. `자바스크립트`를 사용하는 방식입니다.

## 깊이우선 탐색 (DFS : Depth First Search)

- 루트 노드에서 시작하여 최대한 깊이 내려간 후 다음 분기(branch)로 이동하는 탐색 방법입니다.
- 검색속도 자체는 너비우선 탐색보다 느립니다.
- 너비우선 탐색보다 구현 방식이 간단합니다.
- 모든 노드를 방문하고자 할 때 적합한 방식입니다.

→ **하나의 스택과 하나의 큐로 구현합니다.**

→ **어떤 노드를 방문했는지 여부를 검사해야 합니다**.(무한루프 방지)(큐에 저장)

## 너비우선 탐색 (BFS : Breadth First Search)

- 루트 노드에서 시작하여 인접한 노드부터 탐색하는 방법입니다.
- 주로 두 노드 사이의 최단거리를 찾을 때 적합한 방식입니다. (가까운 노드부터 탐색)

→ 두개의 **큐로 구현합니다.**

→ **어떤 노드를 방문했는지 여부를 검사해야 합니다**.(무한루프 방지)(큐에 저장)

## 그래프 방문 예시

![https://s3-us-west-2.amazonaws.com/secure.notion-static.com/cd1239f3-84e7-4284-998a-17b3316f5ff2/Untitled.png](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/cd1239f3-84e7-4284-998a-17b3316f5ff2/Untitled.png)

- 깊이우선 탐색 순서 : A - B - D - E - F - C - G - H - I - J
- 너비우선 탐색 순서 : A - B - C - D - G - H - I - E - F - J

[출처](<[https://www.fun-coding.org/](https://www.fun-coding.org/)>)

## 시간복잡도

- 두 방식 모두 모든 노드를 방문한다는 점에서 시간 복잡도는 동일합니다.
- 그래프를 구현하는 방식에서 차이가 있습니다. (**인접 행렬 vs 인접리스트**)
- 인접 리스트 : O(N+E) (N : 노드 , E : 간선)
- 인접 행렬 : O(N^2)

→ **일반적으로 간선의 크기가 N^2보다 상대적으로 작기 때문에 인접 리스트로 구현하는 것이 더 효율적입니다.**

## JS를 사용하여 구현한 예시 (트리 생성, 탐색)

```jsx
class treeNode {
  constructor(data) {
    this.data = data;
    this.left = null;
    this.right = null;
  }
}

class bst {
  constructor() {
    this.root = null;
  }

  insert(data) {
    let node = new treeNode(data);

    // 루드가 설정되어 있지 않다면 루트를 node로 만들어 준다. node는 treeNode()에서 뼈대를 받아온다.
    if(!this.root) {
      this.root = node;
      return this;
    }

    // 비교를 위해 current 를 설정해 준다.
    let current = this.root;

    // current가 true 라면 while문을 돌면서 data와 지금 현재 data인 current data를 비교한다.
    while (current) {
      // 중복된 값은 어떤 결과를 리턴하지 않는다.
      if(data === current.data) {
        return;
      }
      // data가 기준 data(current data) 보다 작다면 왼쪽에 넣어준다.
      if(data < current.data) {
        if(!current.left) {
          current.left = node;
          break;
        }
      // 이제 current data(기준)는 왼쪽의 data로 잡힌다.
        current = current.left;
      }
      // data가 기준 data(current data) 보다 크다면 오른쪽에 넣어준다.
      if(data > current.data) {
        if(!current.right) {
          current.right = node;
          break;
        }
        // 이제 current data(기준)는 오른쪽 data로 잡힌다.
        current = current.right;
      }
    }
  }

  // Breadth-first Search(너비 우선 탐색)
  bfs() {
    let node = this.root;
    let queue = [node];
    let finalData = [];

    while(queue.length) {
      node = queue.shift();
      if(node.left) {
        queue.push(node.left);
      }
      if(node.right) {
        queue.push(node.right);
      }
      finalData.push(node.data);
    }
    return finalData;
  }

  // Depth-first Search(깊이 우선 탐색)
  preOrder() {
    const finalData = [];
    function traverse(node) {
      finalData.push(node);
      if(node.left) {
        traverse(node.left);
      }
      if(node.right) {
        traverse(node.right);
      }
    }
    traverse(this.root);
    return finalData;
  }
```

## 참고

[참고사이트1](<[https://devuna.tistory.com/32](https://devuna.tistory.com/32)>)

[참고사이트2](<[https://ukcasso.tistory.com/12](https://ukcasso.tistory.com/12)>)
